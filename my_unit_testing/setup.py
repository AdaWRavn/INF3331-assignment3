from distutils.core import setup

""" Setup for the my_unit_testing module. Run 'pythin setup.py install' in command window to install."""

setup(name = 'my_unit_testing',
      version = '1.0',
      description = 'Unit testing for addition_testing.py',
      py_modules=['my_unit_testing']
      )